<div class="min-h-screen hero bg-base-200">
  <div class="flex-col w-full hero-content lg:flex-row-reverse">
    <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="flex-shrink-0 w-full shadow-2xl card bg-base-100">
        <div class="card-body">
          <div class="form-control">
            <%= image_tag "iding_logo.png", class:"block w-36 m-auto md:w-30 xl:w-30" %>
            <div class="px-12 m-auto ">
              <h1 class="my-4 text-2xl font-bold text-right text-gray-600 ">重新發送驗證信件</h1>
            </div>
          </div>  

          <div class="field">
            <%= f.label :email, class: "label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), class: "iding-customer-input", placeholder: "請輸入您的Email" %>
          </div>

          <div class="my-5 actions">
            <%= f.submit "重寄認證信", class: "w-full block bg-white hover:bg-gray-100 focus:bg-gray-100 text-gray-900 font-semibold rounded-lg px-4 py-3 border border-gray-300" %>
          </div>
        <% end %>
        <hr>
        
        <div>
          <p class="font-bold text-gray-700 ">如果您沒有收到驗證信</p>
          <ul>
            <li class="mt-2 text-sm">1. 確認您檢查的電子郵件帳號正確無誤。</li>
            <li class="my-2 text-sm ">2. 請檢查信箱的垃圾郵件和垃圾信件匣。</li>
            <li class="text-sm">3. 短時間內嘗試太頻繁。請稍後再嘗試。</li>
          </ul>
        </div>
      </div>
    </div>  

    <div class="hidden lg:block lg:text-left ">
      <%= image_tag "logo_column.png", class:" w-full py-2 m-auto md:w-30 xl:w-30" %> 
    </div>
</div>






